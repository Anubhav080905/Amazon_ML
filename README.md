# Amazon_ML

### Installation & Local Setup

1. **Clone the repository**
   ```bash
   git clone https://github.com/Anubhav080905/Amazon_ML.git
cd Amazon_ML
git lfs install
git lfs pull
python3 -m venv .venv
.venv\Scripts\activate.bat
pip install -r code\business_entity_resolution\requirements.txt
   ```
